//middleware acts on actions before they hit the reducer. We will need this for thunks
//import action
/*
function forbiddenWordsMiddleware({ getState, dispatch }) {
  return function(next){
    return function(action){
      // do your stuff
      //if action.type ...
      //can send dispatches
      //...return next(action) to continue
      //...return somethign else to avoid original action
      return next(action);
    }
  }
}
*/


